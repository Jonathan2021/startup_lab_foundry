# Local-only defaults. Copy to .env if your selected settings loader supports it.
FOUNDRY_DATABASE_URL=sqlite:///foundry.local.db
FOUNDRY_DEBUG=false

# Keep SQL echo separate from DEBUG: bound values may contain sensitive data.
FOUNDRY_SQL_ECHO=false
